namespace switchboard;

union Content { ViewUpdate, Broadcast }

table Broadcast {
    text:string;
}

table ViewUpdate {
    data:[ubyte];
}

table Msg {
    session:string;
    content:Content;
}

root_type Msg;
